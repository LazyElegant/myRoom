<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cm.dao.ExpressDao">

    <insert id="insert">
        insert into `express` values (#{id}, #{numbers},#{thing},#{weight},#{senderName},#{senderPhone},#{senderAddress},#{addresseeName},#{addresseePhone},#{addresseeAddress},#{status})
    </insert>



    <select id="list" resultType="com.cm.model.Express">
        select id,
        numbers,
        thing,
        weight,
        sender_name 'senderName',
        sender_phone 'senderPhone',
        sender_address 'senderAddress',
        addressee_name 'addresseeName',
        addressee_phone 'addresseePhone',
        addressee_address 'addresseeAddress',
        status
        from `express`
    </select>

    <select id="get" resultType="com.cm.model.Express">
        select id,
        numbers,
        thing,
        weight,
        sender_name 'senderName',
        sender_phone 'senderPhone',
        sender_address 'senderAddress',
        addressee_name 'addresseeName',
        addressee_phone 'addresseePhone',
        addressee_address 'addresseeAddress',
        status
        from `express`
        where `numbers` like  concat('%',concat(#{numbers},'%'))
    </select>

    <delete id="delete">
        delete from `express`
        where id = #{id}
    </delete>

    <update id="update">
        update `express`
        set `status` = 2
        where id = #{id}
    </update>
</mapper>